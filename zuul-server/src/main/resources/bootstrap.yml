spring:
  application:
    name: zuul-server
    index: ${random.long}
  cloud:
    config:
      uri: http://${CONFIG_HOST:localhost}:${CONFIG_PORT:18888}
      name: ms-zuul
      profile: ${PROFILE:dev}